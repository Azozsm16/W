# EB-ABF — Project Context

> Evidence-Based Adaptive Behavioral Firewall
> اقرأ `behavioral-firewall-spec.md` قبل أي عمل. هو المرجع الوحيد للقرارات.

## ما هذا المشروع

وكيل أمني (host agent) على Linux يقيّم **سلوك** البرامج بدل هويتها، ويصدر درجة خطورة + درجة ثقة، ويبني عليهما استجابة متدرجة.

الهدف الذي يميّزه: **تقليل الإنذارات الكاذبة**. أي قرار تصميمي يزيدها مرفوض مهما حسّن الكشف.

## مبادئ غير قابلة للتفاوض

1. **لا تخمين** — لا حكم بلا مصدر يمكن التحقق منه. إن لم يوجد دليل، المخرج `unverified` لا تخمين.
2. **الدرجة ≠ الثقة** — حقلان منفصلان دائماً. الفعل التلقائي يتطلب الاثنين (مصفوفة 8.1).
3. **Decision منفصل عن Enforcement** — القرار يُحسب ويُسجَّل دائماً؛ التنفيذ اختياري وقابل للإيقاف.
4. **Fail-open للتنفيذ، fail-safe للتسجيل** — عطل داخلي لا يُعطّل جهاز المستخدم أبداً.
5. **Metadata فقط** — لا محتوى ملفات، لا نص محادثات، لا ضغطات مفاتيح، لا لقطات شاشة.
6. **ML لا يحجب بمفرده في v1** — يكتشف ويرتّب وينبّه فقط.

## قواعد العمل معي

- **لا تنفّذ خارج الـ Sprint الحالي.** إن رأيت شيئاً ناقصاً، اذكره ولا تبنِه.
- **اسألني عند الغموض** بدل الافتراض. الافتراض الصامت أسوأ من السؤال.
- **اشرح قرارك التقني** في سطر أو سطرين قبل الكود — أحتاج أن أفهم ما أملكه.
- **لا تنسخ كوداً من مشاريع خارجية.** إن استلهمت فكرة، اذكر المصدر في `reference/NOTES.md`.
- **تحقق من واجهة أي مكتبة قبل استخدامها.**
  لا تفترض أسماء خصائص أو دوال من الذاكرة.
  اربط الثوابت بالمكتبة نفسها (استورد `EVENT_TYPE_CREATED`، لا تكتب `"created"`)،
  وثبّت كل افتراض في `tests/test_library_contracts.py` ليكسر ترقيةُ المكتبةِ البناءَ
  بدل أن تُفرغ حقلاً بصمت.
- **اختبار لكل مكوّن** (pytest). مكوّن بلا اختبار غير مكتمل.
- **لا تعدّل قراراً في سجل القرارات** (رأس الوثيقة) دون أن تسألني أولاً.

## القرارات المعمارية التي لا يمكن تداركها لاحقاً

تُنفَّذ في Sprint 1، لا تؤجَّل:

| القرار | السبب |
|---|---|
| Event Schema كامل (بـ `tenant_id`, `raw_score`, `confidence`) | إضافتها لاحقاً = ترحيل قاعدة بيانات |
| فصل Decision / Enforcement | Training Mode مبني عليه |
| عزل تخزين الهوية عن الأحداث | شرط دور Platform Admin |
| Kill-Switch | إيقاف فوري للتنفيذ بلا إعادة تشغيل |

## البيئة

- Python 3.11+ / Ubuntu 22.04+
- `psutil`, `scapy`, `watchdog`, `scikit-learn`, `shap`, `pytest`
- SQLite في v1 → PostgreSQL لاحقاً

## هيكل المجلدات

```
src/            كودي — كل التنفيذ هنا
tests/          pytest
reference/      كود مرجعي للقراءة فقط — لا يُدمج، خارج مسار البناء
experiments/    سجل تجارب ضبط الأوزان
docs/           behavioral-firewall-spec.md
```

## اللغة

- الكود والتعليقات والتزامات git: **إنجليزي**
- الشرح لي في المحادثة: **عربي**
- نصوص المستخدم النهائي: **عربي** (انظر 17.2)
